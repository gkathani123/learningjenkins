pipeline {
    agent {
        docker { run --name test -it baseapim/toolkit }
    }
    stages {
        stage('Test') {
            steps {
                sh 'apic --version'
            }
        }
    }
}
